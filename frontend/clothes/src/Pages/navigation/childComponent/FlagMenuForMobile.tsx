/* 
import type { SupportedLanguages } from "../../../reducerSlices/LangSlice";

interface PropsType {
  flagsMenu: { url: string; language: SupportedLanguages }[];
}
export default function FlagMenuForMobile({ flagsMenu }: PropsType) {
  return <div>FlagMenuForMobile</div>;
} */
